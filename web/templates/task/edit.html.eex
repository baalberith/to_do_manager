<h2>Edit task</h2>

<%= render "form.html", changeset: @changeset,
                        action: list_task_path(@conn, :update, @task.list_id, @task) %>

<%= link "Back", to: list_path(@conn, :show, @task.list_id) %>
